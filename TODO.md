# TODO

- [9/10][persona/voice-reply-latency] **User-voice → persona reply is laggy.** When the user speaks (ambient_mic path), the round-trip to a persona reply is too slow to feel conversational. Suspect: Lane A is the same lane used for *everything* that's not pure background, so screen-share scene-cut wakes (each ~18 s of Doubao) are sharing the queue with user-voice wakes, and Lane A's debounce/preemption may be holding the reply behind in-flight screen work. Investigation: instrument `persona/teacher/triggers.py` Lane A queue with arrival → start-processing → first-token latencies, broken down by event source (`UserSpeechEvent` vs `ScreenSegmentEvent` vs `CanvasEvent`). Likely fix is paired with the next item (move screen perception off Lane A).
- [5/10][persona/reply-length] **Persona replies are too long.** The teacher persona's responses skew lengthy — feels verbose in conversation, especially over voice. Investigate `persona/teacher/prompts/` for length guidance; either tighten the system prompt to bias toward shorter answers (e.g. "default to 1–3 sentences unless explanation is requested") or post-process via a length budget. Voice-mode replies in particular should be short because they're spoken aloud.
- [8/10][screen-share/latency] **Single-in-flight serialization per session.** `services/persona/routers/screen_share.py` currently caps in-flight chunks at 3 (`infra/media/screen_session._MAX_IN_FLIGHT_CHUNKS = 3`), but with Doubao taking ~18 s/frame and chunks arriving every 3 s, processing accumulates: 30 s of sharing → ~2 min of perceived lag. Also: the in-memory `_SESSIONS` dict gets wiped by uvicorn `--reload`, so even the 3-slot cap doesn't fire reliably in dev. Replace with cap=1 + immediate drop on overflow (record a "skipped: still processing prior chunk" segment so the persona sees the gap). The persona getting *fresh* perception matters more than complete history.
- [7/10][screen-share/latency] **Drop image to 384 px on the screen-share path.** `infra/media/frame_select.dedupe_and_resize` defaults to `max_edge=768`. For screen-share specifically, 384 px is enough for Doubao to recognise UI state ("VSCode with a terminal pane open", "Chrome on a Stack Overflow page") — halves the token cost, ~halves the per-frame latency from ~18 s to ~9 s. Wire a per-call `max_edge` through `describe_video` so VOD keeps 768 and the screen-share router passes 384.
- [7/10][screen-share/latency] **Real cross-chunk pHash dedup.** `services/persona/routers/screen_share.py` currently does cross-chunk dedup by comparing the Doubao *output strings* (`seg.content == session.last_kept_phash`). That never hits because Doubao varies wording on identical frames. The orchestrator already pHashes within a chunk via `infra/media/frame_select.dedupe_and_resize`; teach it to surface the kept frames' pHashes alongside the segments, then in the router compare `imagehash.hex_to_hash(seg.phash) - session.last_kept_phash <= 6` and skip the Doubao call entirely on near-duplicate frames. Static screens (active typing in a textbox) should cost ~zero Doubao time.
- [3/10][infra/media] Video understanding is implemented as ffmpeg keyframe-extraction + per-frame `describe_image` + Whisper transcript, merged into a chronological timeline (`infra/media/video.py`). This is the cross-provider fallback and the natural fit for near-live streaming. Graduate to a realtime sidecar (Doubao Realtime / Gemini Live / GPT-4o Realtime over WebSocket) when a use case actually needs sub-second live latency; the frame-extraction tool stays useful as the provider-agnostic fallback.
- [4/10][research/evaluate-fallback] In benchmark scenario 2 (HTTP/2), Lane R still issues ~3 `browser_set(action='evaluate')` calls when hunting for a direct quote inside a Wikipedia section. snapshot+@ref handles structural navigation but not "find the paragraph containing string X". Add a new action `browser_set(action='find_text', query=...)` that scans the snapshot tree text for headings/paragraphs containing the query and returns the best matching @ref. ~30 lines in `services/browser/main.py`. Lower the per-loop `evaluate` ceiling at the same time (research_policy.md cap) so the LLM is forced toward find_text.
- [7/10][research/embedding-collapse] **Investigate the nomic-embed-text short-input collapse bug** that surfaced in the recipe-replay generalization test (`tests/manual/test_recipe_generalization.py`). Observed: `embed_text("Genghis Khan")` and `embed_text("King Arthur")` produce **literally identical 768-dim vectors** (cosine = 1.0, byte-for-byte equal). Longer inputs like `embed_text("Genghis Khan was a 13th-century Mongol emperor whose conquests reshaped Europe")` and `embed_text("King Arthur was a legendary Welsh-British figure...")` differentiate fine. Verified by direct Ollama API hit (the collapse happens inside the model, not our code). For recipe lookup this is currently a *helpful* accident — same-shape questions hit the same recipe — but it's fragile and probabilistically wrong. We need to understand it before relying on it. Investigation plan: (1) read nomic-embed-text's tokenizer + pooling behavior; is it mean-pooling and the proper-noun tokens get drowned out when common-vocabulary "impact on Europe of" dominates? (2) reproduce on `mxbai-embed-large` and `bge-base-en` to see if it's universal to small embedding models or specific to nomic; (3) measure the threshold of input length at which proper nouns start contributing; (4) decide between: (a) prepend a distinctive prefix (the URL slug or host) to embedded text — cheapest, ~3 lines; (b) swap to a model that handles short inputs (heavier — re-embed all stored data); (c) use a hybrid: embedding gate AND a structural shape hash (e.g. POS-tag template). Files to touch when fixing: `infra/rag/embedding.py` (the embed_text call site), `workshop/research/recipes.py:record_after_success` (where the goal is embedded for storage), `workshop/research/recipe_store.py:lookup` (where the runtime goal is embedded for match). Until investigated, the recipe lookup threshold (`_THRESHOLD = 0.85` in `recipe_store.py`) is on the edge of "matches too much" — leave it but document the risk.
- [3/10][research/skillify-followups] Codified research recipes shipped (`workshop/research/`). Per-user JSON storage at `data/research/<user_id>/recipes/`. Lookup-by-(host+goal-embedding cosine ≥ 0.85). Smoke test ≥70% ARIA-ref match. Replay = data collection + one `generate_cached` synthesis. Recording fires in `_execute_research` finally on successful synthesis. Follow-ups still open: (a) cross-user / promote-to-global tier; (b) explicit quarantine + N-replay promotion; (c) recipe edit/delete UI; (d) `find_text` action (see above) so HTTP/2-style quote hunts replay cleanly; (e) variable inference smarter than URL/@ref heuristics (e.g. extract a $ticker from goal text and slot it into URL query params).
- [6/10][research/domain-skills] **Per-host prose notes** (gstack domain-skills pattern). Lane R loop authors a one-paragraph "what I learned about navigating this site" note keyed by URL host, e.g. `finance.google.com.md`: "headline numbers are in `.PriceValue`; news is the .news-section list; sector comparison sidebar uses `.sector-perf` (collapsed by default — call expand=true)." Notes feed into the research prompt's `dynamic_user` when the goal mentions a URL whose host has a note. Quarantine → active after N=3 uses without error, optionally promote to global. Files: new `persona/teacher/domain_skills.py` (read/write/lifecycle), feed into `persona/teacher/prompts/research.py:build()`, write trigger in `_execute_research` finally (LLM emits a note as part of synthesis if it found patterns worth saving). Storage: `data/domain-skills/<host>.md` per-user, `~/.bewithme/domain-skills/<host>.md` global. Cheaper than #1 because no embedding match needed — exact host match is fine.
- [8/10][goal-planning] Goal planning DAG is broken — not usable for real learning paths. The LLM-generated prerequisites are hard to ground in reality. Nodes like "Learn CSS fundamentals" or "Practice self-regulation" are too abstract to act on reliably. The expand/atomic judgment is inconsistent. Needs a fundamental rethink: possibly constrain to known curricula, real courses, or verified learning resources instead of freeform LLM decomposition.
- [4/10][frontend/pdf-viewer] PDF outline navigation uses text-matching hack (Reader.tsx handleSectionClick). Replace with built-in react-pdf/pdfjs-dist scroll-to-destination API if one becomes available.
- [7/10][desktop] Playwright handoff still pops up a separate Chromium window on the Mac (the existing backend flow — see `app/api/browser.py`). Phase 2: stream the Playwright page into a middle panel **inside the app** (likely CDP screencast + input forwarding, with Playwright switched to headless) so the "Open in Browser" flow stays in-window.
- [5/10][frontend/reader] In inline-browser mode the left exploration panel is empty because there is no pre-extracted passage text to parse an outline from. Summarize the live page (feed `uploadUrl` background result into an LLM summary + outline) and render that in the panel, so users get structure/navigation for a live URL the same way they do for a pasted passage.
- [4/10][frontend/canvas-grid] Mid-session device-class change (e.g. window resize crossing the tablet/phone breakpoint, mobile rotation) doesn't reflow existing blocks. New mounts honor the new grid; already-mounted blocks keep their previous coords and can clip or overlap. Fix: subscribe to useDeviceClass changes in DynamicSurface and either trigger the teacher to replan or reproportionally rescale every block's inline grid via dynamicBlockRegistry.setGrid.
