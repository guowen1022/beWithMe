# TODO

- [8/10][goal-planning] Goal planning DAG is broken — not usable for real learning paths. The LLM-generated prerequisites are hard to ground in reality. Nodes like "Learn CSS fundamentals" or "Practice self-regulation" are too abstract to act on reliably. The expand/atomic judgment is inconsistent. Needs a fundamental rethink: possibly constrain to known curricula, real courses, or verified learning resources instead of freeform LLM decomposition.
- [4/10][frontend/pdf-viewer] PDF outline navigation uses text-matching hack (Reader.tsx handleSectionClick). Replace with built-in react-pdf/pdfjs-dist scroll-to-destination API if one becomes available.
- [7/10][desktop] Playwright handoff still pops up a separate Chromium window on the Mac (the existing backend flow — see `app/api/browser.py`). Phase 2: stream the Playwright page into a middle panel **inside the app** (likely CDP screencast + input forwarding, with Playwright switched to headless) so the "Open in Browser" flow stays in-window.
- [5/10][frontend/reader] In inline-browser mode the left exploration panel is empty because there is no pre-extracted passage text to parse an outline from. Summarize the live page (feed `uploadUrl` background result into an LLM summary + outline) and render that in the panel, so users get structure/navigation for a live URL the same way they do for a pasted passage.
