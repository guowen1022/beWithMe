"""Render helpers for persona-authored visual artifacts.

Right now this is the home of the Mermaid → SVG pipeline used by the
`note` block, plus the HTML preprocessor that runs persona output
through sanitization + diagram resolution before it lands on a client.
"""
