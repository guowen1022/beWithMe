fastapi>=0.100.0,<0.116.0
uvicorn>=0.23.0
watchfiles>=0.20  # used by uvicorn --reload; without it, StatReload polls every file's mtime and burns CPU
httpx>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.28.0
pgvector>=0.2.0
alembic>=1.12.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
pyyaml>=6.0
anthropic>=0.30.0
openai>=1.50.0
pypdf>=4.0.0
playwright>=1.45.0
trafilatura>=1.12.0
networkx>=3.0  # persona/teacher/knowledge/graph.py — concept-graph walks. Was missing entirely: the knowledge sidecar could not boot from a clean install.
numpy>=1.24  # services/speak + services/transcribe audio buffers. Arrived transitively via onnxruntime; declared now because both import it directly.
lxml>=5.0  # infra/render/note.py, infra/render/svg_inline_css.py, workshop/canvas/tools/edit_note.py. Arrived transitively via trafilatura.
pytest>=8.0.0
pytest-asyncio>=0.23.0
pywhispercpp>=1.2.0
kokoro-onnx>=0.4.0
onnxruntime>=1.17.0  # LiveKit turn-detector (EOU) ONNX session in services/transcribe
tokenizers>=0.19.0   # HF tokenizer for the EOU model, no torch dep
Pillow>=10.0.0
imagehash>=4.3.1
webrtcvad-wheels>=2.0.14
nh3>=0.2.18  # HTML sanitizer used by infra/render/note.py — Rust-backed ammonia bindings
markdown-it-py>=3.0.0,<4.0.0  # writer authors in markdown; note_md.py renders to HTML
mdit-py-plugins>=0.4.0,<0.5.0  # ==mark== highlight plugin + future extensions
